{
  "name": "sono-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "vite build",
    "deploy": "vite build && wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply sono-worker-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply sono-worker-db --remote"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "sono-editor": "file:../sono-editor"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "^5.7.0",
    "vite": "^7.0.4",
    "wrangler": "^4.14.0"
  }
}
